{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
                
            <h1>Resultado para a cidade {{ cidade.nome }}</h1>

            <div id="catlist">
                {% for carro in listaCarros %}
                <dl>
                    <dt><img src="http://placehold.it/300x200" alt="Product image" width="93" height="62" /><strong>Ano: {{ carro.ano|date("Y")}}</strong><a href="#">{{ carro.marca }} - {{ carro.modelo }}</a></dt>
                    <dd>
                        <p>Cor: {{ carro.cor }}</p>
                        <p>Categoria: {{ carro.categoria }}</p>
                        <p>Preço: </p>
                    </dd>
                </dl>
                {% endfor %}
            </div>

        </div>
    </div>    

{% endblock %}

{% block javascripts %}



{% endblock %}

{% block stylesheets %}
    <style>
        /* box model hacks not catered for in the demo */
        /* global reset for demo purposes only - use a proper reset in a real page*/
        *{margin:0;padding:0}
        p{margin:0 0 .5em 0}
        h1{margin:1em 0;text-align:center}
        #catlist{
            border:1px dashed #ccc;
            border-bottom:none;
            width:660px;
            margin:10px auto;
        }
        #catlist dl{
            width:640px;
            margin:0 auto;
            border-bottom:1px dashed #ccc;
            padding:10px;
            overflow:hidden;
            background:#f2f2f2;
        }
        #catlist dd{overflow:auto}
        #catlist dt strong{
            float:right;
            padding:0 0 0 20px;
        }
        #catlist dt img{
            float:left;
            margin:0 10px 0 0;
            border:1px solid #000;
        }
        * html dd{height:1%}/* 3px jog*/        

    </style>

{% endblock %}